{
  "name": "capi-private-preview",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild client.js --bundle --minify --outdir=dist --loader:.css=css",
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "lint": "biome check server.js client.js test"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.10.0",
    "@xterm/xterm": "5.5.0",
    "node-pty": "1.1.0",
    "ws": "8.21.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.8",
    "esbuild": "0.25.12"
  }
}
